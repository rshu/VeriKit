# Attribution / 致谢

[English](#english) · [中文](#中文)

---

## English

VeriKit incorporates technical documentation from the HarmonyOS SDK, © Huawei Device Co., Ltd. The reference doc bodies under `skills/<kit>/references/*.md` are extracted verbatim or near-verbatim from Huawei's published HarmonyOS developer documentation, and remain Huawei's copyright. They are redistributed in this repository solely to make them locatable by the VeriKit Kit-selection ranker; refer to the upstream HarmonyOS Developer site for authoritative versions, and to Huawei's terms of use for HarmonyOS documentation for any other use.

The contract files (`dist/contracts/*.json`), reference manifests (`dist/manifests/*.json`), composition recipes (`dist/compositions/recipes.json`), augmented SKILL.md frontmatter (`skills/<kit>/SKILL.md`), ranker / CLI / hook / slash command code (`bin/`, `hooks/`, `commands/`, `dist/verikit-cli.js`), and build scripts are original VeriKit work, licensed MIT — see [`LICENSE`](./LICENSE).

The 100 Kit identifiers (`ability-kit`, `arkdata`, `arkui`, …) and the "HarmonyOS" / "ArkTS" names are trademarks of Huawei Device Co., Ltd., referenced here for the purpose of identifying the SDK components VeriKit selects against.

If you are the copyright owner of any included material and believe its inclusion here exceeds fair use or your published redistribution terms, please open an issue at https://github.com/rshu/VeriKit — we will take appropriate action.

---

## 中文

VeriKit 集成了来自 HarmonyOS SDK 的技术文档，© 华为终端有限公司。`skills/<kit>/references/*.md` 下的参考文档正文来自华为公开发布的 HarmonyOS 开发者文档，逐字或近乎逐字引用，版权归华为所有。本仓库重新分发这些文档仅为方便 VeriKit Kit 选择排序器对其定位；以官方 HarmonyOS 开发者站点上的版本为准，任何其他用途请遵循华为关于 HarmonyOS 文档的使用条款。

合约文件（`dist/contracts/*.json`）、参考清单（`dist/manifests/*.json`）、组合配方（`dist/compositions/recipes.json`）、增强的 SKILL.md frontmatter（`skills/<kit>/SKILL.md`）、排序器 / CLI / 钩子 / 斜杠命令代码（`bin/`、`hooks/`、`commands/`、`dist/verikit-cli.js`）以及构建脚本均为 VeriKit 原创内容，采用 MIT 协议授权 —— 详见 [`LICENSE`](./LICENSE)。

100 个 Kit 名称（`ability-kit`、`arkdata`、`arkui` 等）以及 "HarmonyOS" / "ArkTS" 名称是华为终端有限公司的商标，在此处仅为标识 VeriKit 所选择的 SDK 组件而引用。

如果您是上述任何材料的版权所有人，并认为本仓库的收录超出了合理使用范围或您公开的再分发条款，请在 https://github.com/rshu/VeriKit 上提交 issue —— 我们会进行相应处理。
