---
name: ipc-kit
description: IPC Kit（进程间通信服务）为HarmonyOS提供IPC和RPC能力，基于Binder和软总线驱动实现跨进程和跨设备的数据传输与远程过程调用。
---

## 功能说明

IPC Kit（进程间通信服务）为 HarmonyOS 提供 IPC（设备内进程间通信）和 RPC（跨设备远程过程调用）能力。IPC Kit 采用客户端-服务端（Client-Server）模型，客户端进程通过 Proxy 对象向服务端 Stub 对象发起请求并获取应答，实现跨进程通信。

IPC Kit 支持 ArkTS 和 C/C++ 两套开发接口。IPC 使用 Binder 驱动适用于设备内通信，RPC 使用软总线驱动适用于跨设备通信。典型使用场景包括后台服务（单设备跨进程调用）和多端协同（跨设备远程调用）。

IPC Kit 还提供了远端状态订阅机制，可以在远端 Stub 对象死亡时自动触发回调通知，方便开发者及时处理连接异常。单个设备上跨进程通信时传输数据量最大为 200KB，超过时可使用匿名共享内存。

## 使用场景

当您需要实现设备内的跨进程通信（如后台服务与前台应用之间的接口调用和数据传递）时，可使用 IPC Kit 的 IPC 能力。

当您需要实现跨设备的远程过程调用（如多端协同场景下的远端接口调用和数据传递）时，可使用 RPC 能力。当您需要监听远端服务进程的死亡状态以便及时处理连接中断时，可使用远端状态订阅能力。

## 典型场景

- 如何使用ArkTS实现IPC跨进程通信，包括Proxy和Stub的开发？
- 如何使用C/C++实现IPC/RPC跨进程通信？
- 如何实现远端Stub对象的死亡通知订阅？
- 如何在后台服务中通过IPC机制提供跨进程接口调用？
- 如何在多端协同场景中通过RPC实现跨设备远程调用？
- 单次IPC通信的数据量超过200KB时如何使用匿名共享内存传输？

## 使用指南

**重要提示：** 本Kit的references目录下包含大量详细参考文档。在查询时，请根据用户的具体问题，仅加载references中与问题相关的MD文件进行阅读，不要一次性加载所有文件，以免上下文过长影响回答质量。先通过标题和描述判断哪些文件与当前问题相关，再针对性地读取。

## 参考文档

### 简介

- [IPC Kit简介](references/IPC Kit简介.md) - IPC Kit的基本概念、实现原理和约束限制说明

### 开发指导

- [IPC与RPC通信开发指导(ArkTS)](references/IPC与RPC通信开发指导(ArkTS).md) - ArkTS实现IPC/RPC跨进程通信的完整开发指导
- [IPC与RPC通信开发指导(C/C++)](references/IPC与RPC通信开发指导(C_C++).md) - C/C++实现IPC/RPC跨进程通信的完整开发指导
- [远端状态订阅开发实例](references/远端状态订阅开发实例.md) - 订阅远端Stub对象死亡通知的开发实例

<!-- verikit:references-index -->

## References / 参考文档索引

_Auto-generated by VeriKit from this Kit's reference manifest — `4` document(s) shipped under `references/`. Each link points at a real file in the plugin tree; load any with the agent's file-read tool._

- **IPC Kit简介** — - 使用IPC和RPC进行跨进程通信时，需要先调用元能力的连接服务接口获取Proxy对象。IPC和RPC的典型使用场景包括： - IPC典型使用场景是后台服务，后台服务通过IPC机制提供单设备跨进程接口调用与数据传递能力。 - RPC典型使用场景是多端协同，多端协同通过RPC机制提供跨设备远端接口调用与数据传递能力。 #### 实现原理 IPC和RPC用于实… [`references/IPC Kit简介.md`](<references/IPC Kit简介.md>)
- **IPC与RPC通信开发指导(ArkTS)** — IPC/RPC的主要工作是跨进程建立对象通信的连接（客户端进程的Proxy和服务端进程的Stub建立一一对应关系），从而通过Proxy的接口可以和Stub进行IPC/RPC通信。 #### 开发步骤 - 在进行IPC&RPC跨进程通信前需要通过Ability Kit获取服务端的代理对象。 - 不支持三方应用实现跨进程通信，三方应用仅可通过 connectSe… [`references/IPC与RPC通信开发指导(ArkTS).md`](<references/IPC与RPC通信开发指导(ArkTS).md>)
- **IPC与RPC通信开发指导(C/C++)** — IPC让运行在不同进程间的Proxy和Stub实现互相通信。IPC CAPI是IPC Kit提供的C语言接口。 IPC CAPI接口不直接提供获取通信代理对象的能力，该功能由 Ability Kit 提供。 进程间IPC通道的建立，请参考 Native子进程开发指导（C/C++） 。本文重点介绍IPC CAPI的使用。 #### 接口说明 **表1** IP… [`references/IPC与RPC通信开发指导(C_C++).md`](<references/IPC与RPC通信开发指导(C_C++).md>)
- **远端状态订阅开发实例** — IPC/RPC提供了订阅远端Stub对象状态的机制。当远端Stub对象死亡时，可以自动触发本端Proxy注册的死亡通知。这种死亡通知订阅需要调用指定接口 registerDeathRecipient 完成。不再需要订阅时，也需要调用指定接口 unregisterDeathRecipient 取消订阅。 使用这种订阅机制的用户需要继承死亡通知类 DeathRe… [`references/远端状态订阅开发实例.md`](<references/远端状态订阅开发实例.md>)
